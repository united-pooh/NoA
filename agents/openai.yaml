interface:
  display_name: "NoA（生盐诺亚）"
  short_description: "用 MCP 轨迹、证据和负面结果维护可复现的研究过程"
  default_prompt: "Use $noa to maintain an evidence-driven research trajectory through the NoA MCP kernel, then export a readable Markdown record with explicit observations, interpretations, decisions, branches, and reproducibility details."
